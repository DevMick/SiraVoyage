{
  "name": "sira-voyage",
  "version": "1.0.0",
  "description": "Site web de voyages pour Omra et Hajj - AL BAYT",
  "main": "app.js",
  "homepage": ".",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "npm run build:css",
    "build:css": "npx tailwindcss -i ./css/style.css -o ./public/css/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run build:css",
    "predeploy": "node scripts/pre-deploy-check.js",
    "deploy": "npm run predeploy && git add . && git commit -m 'Deploy to production' && git push origin main",
    "test:ftp": "node scripts/test-ftp.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "compression": "^1.7.4",
    "helmet": "^7.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "tailwindcss": "^3.3.6"
  },
  "keywords": [
    "omra",
    "hajj",
    "voyage",
    "pelerinage",
    "al-bayt",
    "sira-voyage"
  ],
  "author": "SiraVoyage",
  "license": "MIT"
}
