name: Deploy to Namecheap

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build CSS
      run: npm run build:css

    - name: Debug FTP Configuration
      run: |
        echo "🔍 Configuration FTP Debug:"
        echo "FTP Host: ${{ secrets.FTP_HOST }}"
        echo "FTP Username: ${{ secrets.FTP_USERNAME }}"
        echo "FTP Password: [HIDDEN]"
        echo "Local directory: ./public/ (fichiers statiques)"
        echo "Server directory: /public_html/ (répertoire web)"
        echo "📁 Contenu du dossier public à déployer:"
        ls -la ./public/
        echo "🔍 Recherche de répertoires public* :"
        find . -type d -name "*public*" 2>/dev/null || echo "Aucun répertoire public trouvé"

    - name: Test FTP Connection and List Directories
      run: |
        echo "🔍 Test de connexion FTP et exploration des répertoires..."
        # Installation de lftp pour tester la connexion
        sudo apt-get update && sudo apt-get install -y lftp

        # Test de connexion et listage des répertoires
        lftp -c "
        set ftp:ssl-allow no;
        open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_HOST }};
        echo '📁 Répertoire racine du compte FTP:';
        ls -la;
        echo '🔍 Recherche de public_html:';
        ls -la | grep public || echo 'Aucun répertoire public_html trouvé à la racine';
        echo '📂 Tentative d accès à public_html:';
        cd public_html 2>/dev/null && ls -la || echo 'Impossible d accéder à public_html';
        quit;
        "
      
    - name: Prepare static files for deployment
      run: |
        echo "📁 Vérification du dossier public après build CSS:"
        ls -la ./public/
        echo "🎨 Vérification que le CSS a été compilé:"
        ls -la ./public/css/ || echo "Dossier CSS non trouvé"
        echo "✅ Les fichiers statiques sont prêts dans ./public/"
        
    - name: Deploy to Namecheap via FTP
      if: github.ref == 'refs/heads/main'
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: 21
        protocol: ftp
        local-dir: ./public/
        server-dir: /public_html/
        timeout: 60000
        log-level: verbose
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.env
          **/.DS_Store
          **/Thumbs.db
          **/package-lock.json
          
    - name: Notify deployment status
      if: always()
      run: |
        if [ ${{ job.status }} == 'success' ]; then
          echo "✅ Déploiement réussi sur omrahajjabidjan.com"
        else
          echo "❌ Échec du déploiement"
          exit 1
        fi
